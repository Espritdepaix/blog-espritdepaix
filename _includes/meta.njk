<ul class="meta">
<li class="date"><cite><a href="{{post.url }}"> {{ title }}</cite></a> a été pubié le <time>{{ page.date | readableDate }} </time>dans la catégorie 
{% for tag in tags -%}
 {% if tag !== 'posts' %}
<a href="/tags/{{ tag | slugify }}/" rel="category tag">{{tag}}</a>, &nbsp;
{% endif %}
{%- endfor %}
</li>
<li>Faire un commentaire, poser une question, c'est<a href="mailto:adresse@email.com?subject={{title}}"> ici </a>que cela se passe.</li>
</ul>


